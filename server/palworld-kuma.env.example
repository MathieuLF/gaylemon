# Copier vers /etc/palworld/kuma.env sur le serveur.
# Garder le fichier reel lisible uniquement par root/steam: l'URL contient le jeton Push Uptime Kuma.
KUMA_PUSH_URL="https://uptime.example.com/api/push/REPLACE_ME?status=up&msg=OK&ping="
